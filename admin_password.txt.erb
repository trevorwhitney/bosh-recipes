<%
  require 'yaml'
  bosh_deployment_vars_path = "#{ENV['privates_dir']}/#{ENV['project_id']}/bosh-deployment-vars.yml"
  admin_password = YAML.load_file(bosh_deployment_vars_path)['admin_password']
%>
<%= admin_password %>
