<%
  require 'yaml'
  bosh_deployment_vars_path = "#{ENV['privates_dir']}/#{ENV['project_id']}/bosh-deployment-vars.yml"
  root_ca = YAML.load_file(bosh_deployment_vars_path)['root_ca']
%>
<%= root_ca['certificate'] %>

